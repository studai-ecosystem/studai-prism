export default function SectionLabel({ text }) {
  return (
    <span className="inline-block text-xs font-semibold tracking-[0.2em] text-[var(--color-accent)] uppercase mb-4 font-sans">
      {text}
    </span>
  )
}
