# Dependencies
node_modules/
server/node_modules/

# Build output
dist/

# Environment files (never commit these)
.env
.env.local
.env.production
server/.env

# Runtime data (user accounts, assessment sessions) — never commit.
# content.json is seed content (blog/careers) and IS committed.
server/data/users.json
server/data/assessments.json

# Logs
*.log
npm-debug.log*

# Browser audit artifacts (generated evidence; CI uploads these separately)
test-results/
playwright-report/
audit-results/

# OS
.DS_Store
Thumbs.db
